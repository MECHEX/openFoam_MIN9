FoamFile
{
    version     2.0;
    format      ascii;
    class       volVectorField;
    object      U;
}

// V2_confined TEMPLATE — values generated by V2Study.py prepare
// Inlet: Poiseuille  u(y) = U_max*(1 - (2y/H)^2)
// Re = U_max*D/nu  (Sahin & Owens 2004 convention)
// DO NOT edit manually — regenerate with: python V2Study.py prepare --overwrite

dimensions      [0 1 -1 0 0 0 0];
internalField   uniform (0.119 0 0);  // placeholder U_max for Re~94 @ beta=0.30

boundaryField
{
    inlet
    {
        type        exprFixedValue;
        valueExpr   "vector(0.119*(1.0 - pow(2.0*pos().y()/0.040, 2.0)), 0, 0)";
        value       uniform (0 0 0);
    }
    outlet   { type zeroGradient; }
    top      { type noSlip; }
    bottom   { type noSlip; }
    cylinder { type noSlip; }
    front    { type empty; }
    back     { type empty; }
}
