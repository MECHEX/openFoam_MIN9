# PoC Analyses

This directory stores proof-of-concept analyses that sit next to the main
`VV_cases` workflow but still reuse its archived data and documentation.

Current PoC:

- `run008_wall_heat_flux_tendency_budget.py`
  Near-wall first-cell estimate of the wall-heat-flux tendency budget for
  `V4b_3D run008`.
